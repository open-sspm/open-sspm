{{- if .Values.discoveryWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-worker-discovery" (include "open-sspm.fullname" .) | trunc 63 | trimSuffix "-" }}
  labels:
    {{- include "open-sspm.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker-discovery
spec:
  replicas: {{ .Values.discoveryWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "open-sspm.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker-discovery
  template:
    metadata:
      {{- with .Values.discoveryWorker.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "open-sspm.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker-discovery
        {{- with .Values.discoveryWorker.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "open-sspm.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: open-sspm
          image: "{{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - worker-discovery
          ports:
            - name: metrics
              containerPort: {{ .Values.metrics.port }}
              protocol: TCP
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ required "database.existingSecret.name is required" .Values.database.existingSecret.name }}
                  key: {{ required "database.existingSecret.key is required" .Values.database.existingSecret.key }}
            - name: HTTP_ADDR
              value: {{ .Values.config.httpAddr | quote }}
            - name: METRICS_ADDR
              value: {{ .Values.config.metricsAddr | quote }}
            - name: AUTH_COOKIE_SECURE
              value: {{ ternary "1" "0" .Values.config.authCookieSecure | quote }}
            - name: DEV_SEED_ADMIN
              value: {{ ternary "1" "0" .Values.config.devSeedAdmin | quote }}
            - name: RESYNC_ENABLED
              value: {{ ternary "1" "0" .Values.config.resyncEnabled | quote }}
            - name: SYNC_DISCOVERY_INTERVAL
              value: {{ .Values.config.syncDiscoveryInterval | quote }}
            - name: SYNC_OKTA_WORKERS
              value: {{ .Values.config.syncOktaWorkers | quote }}
            - name: SYNC_GITHUB_WORKERS
              value: {{ .Values.config.syncGithubWorkers | quote }}
            - name: SYNC_DATADOG_WORKERS
              value: {{ .Values.config.syncDatadogWorkers | quote }}
            {{- with .Values.discoveryWorker.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.discoveryWorker.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.discoveryWorker.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.discoveryWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.discoveryWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.discoveryWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
