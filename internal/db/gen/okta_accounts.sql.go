// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: okta_accounts.sql

package gen

import (
	"context"
)

const countOktaAccounts = `-- name: CountOktaAccounts :one
SELECT count(*)
FROM accounts
WHERE source_kind = 'okta'
  AND expired_at IS NULL
  AND last_observed_run_id IS NOT NULL
`

func (q *Queries) CountOktaAccounts(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countOktaAccounts)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countOktaAccountsByQueryAndState = `-- name: CountOktaAccountsByQueryAndState :one
SELECT count(*)
FROM accounts
WHERE
  source_kind = 'okta'
  AND expired_at IS NULL
  AND last_observed_run_id IS NOT NULL
  AND (
    $1::text = ''
    OR email ILIKE ('%' || $1::text || '%')
    OR display_name ILIKE ('%' || $1::text || '%')
    OR external_id ILIKE ('%' || $1::text || '%')
  )
  AND (
    $2::text = ''
    OR ($2::text = 'active' AND lower(status) = 'active')
    OR ($2::text = 'inactive' AND lower(status) <> 'active')
  )
`

type CountOktaAccountsByQueryAndStateParams struct {
	Query string `json:"query"`
	State string `json:"state"`
}

func (q *Queries) CountOktaAccountsByQueryAndState(ctx context.Context, arg CountOktaAccountsByQueryAndStateParams) (int64, error) {
	row := q.db.QueryRow(ctx, countOktaAccountsByQueryAndState, arg.Query, arg.State)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getOktaAccount = `-- name: GetOktaAccount :one

SELECT id, source_kind, source_name, external_id, email, display_name, raw_json, created_at, updated_at, last_login_at, last_login_ip, last_login_region, seen_in_run_id, seen_at, last_observed_run_id, last_observed_at, expired_at, expired_run_id, status, account_kind, entity_category
FROM accounts
WHERE id = $1
  AND source_kind = 'okta'
  AND expired_at IS NULL
  AND last_observed_run_id IS NOT NULL
`

// Okta account query names on top of accounts.
func (q *Queries) GetOktaAccount(ctx context.Context, id int64) (Account, error) {
	row := q.db.QueryRow(ctx, getOktaAccount, id)
	var i Account
	err := row.Scan(
		&i.ID,
		&i.SourceKind,
		&i.SourceName,
		&i.ExternalID,
		&i.Email,
		&i.DisplayName,
		&i.RawJson,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.LastLoginAt,
		&i.LastLoginIp,
		&i.LastLoginRegion,
		&i.SeenInRunID,
		&i.SeenAt,
		&i.LastObservedRunID,
		&i.LastObservedAt,
		&i.ExpiredAt,
		&i.ExpiredRunID,
		&i.Status,
		&i.AccountKind,
		&i.EntityCategory,
	)
	return i, err
}

const listOktaAccountsPageByQueryAndState = `-- name: ListOktaAccountsPageByQueryAndState :many
SELECT id, source_kind, source_name, external_id, email, display_name, raw_json, created_at, updated_at, last_login_at, last_login_ip, last_login_region, seen_in_run_id, seen_at, last_observed_run_id, last_observed_at, expired_at, expired_run_id, status, account_kind, entity_category
FROM accounts
WHERE
  source_kind = 'okta'
  AND expired_at IS NULL
  AND last_observed_run_id IS NOT NULL
  AND (
    $1::text = ''
    OR email ILIKE ('%' || $1::text || '%')
    OR display_name ILIKE ('%' || $1::text || '%')
    OR external_id ILIKE ('%' || $1::text || '%')
  )
  AND (
    $2::text = ''
    OR ($2::text = 'active' AND lower(status) = 'active')
    OR ($2::text = 'inactive' AND lower(status) <> 'active')
  )
ORDER BY id ASC
LIMIT $4::int
OFFSET $3::int
`

type ListOktaAccountsPageByQueryAndStateParams struct {
	Query      string `json:"query"`
	State      string `json:"state"`
	PageOffset int32  `json:"page_offset"`
	PageLimit  int32  `json:"page_limit"`
}

func (q *Queries) ListOktaAccountsPageByQueryAndState(ctx context.Context, arg ListOktaAccountsPageByQueryAndStateParams) ([]Account, error) {
	rows, err := q.db.Query(ctx, listOktaAccountsPageByQueryAndState,
		arg.Query,
		arg.State,
		arg.PageOffset,
		arg.PageLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Account
	for rows.Next() {
		var i Account
		if err := rows.Scan(
			&i.ID,
			&i.SourceKind,
			&i.SourceName,
			&i.ExternalID,
			&i.Email,
			&i.DisplayName,
			&i.RawJson,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastLoginAt,
			&i.LastLoginIp,
			&i.LastLoginRegion,
			&i.SeenInRunID,
			&i.SeenAt,
			&i.LastObservedRunID,
			&i.LastObservedAt,
			&i.ExpiredAt,
			&i.ExpiredRunID,
			&i.Status,
			&i.AccountKind,
			&i.EntityCategory,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
