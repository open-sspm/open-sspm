// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: saas_apps.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countSaaSAppsByFilters = `-- name: CountSaaSAppsByFilters :one
WITH configured_sources AS (
  SELECT
    k.kind AS source_kind,
    n.name AS source_name
  FROM unnest($3::text[]) WITH ORDINALITY AS k(kind, ord)
  JOIN unnest($4::text[]) WITH ORDINALITY AS n(name, ord) USING (ord)
),
scoped_app_ids AS (
  SELECT DISTINCT sas.saas_app_id
  FROM saas_app_sources sas
  JOIN configured_sources cfg
    ON lower(trim(cfg.source_kind)) = lower(trim(sas.source_kind))
   AND lower(trim(cfg.source_name)) = lower(trim(sas.source_name))
  WHERE sas.expired_at IS NULL
    AND sas.last_observed_run_id IS NOT NULL
    AND (
      $5::text = ''
      OR lower(trim(sas.source_kind)) = lower(trim($5::text))
    )
    AND (
      $6::text = ''
      OR lower(trim(sas.source_name)) = lower(trim($6::text))
    )
),
posture_inputs AS (
  SELECT
    spi.id, spi.canonical_key, spi.display_name, spi.primary_domain, spi.vendor_name, spi.first_seen_at, spi.last_seen_at, spi.created_at, spi.updated_at, spi.owner_identity_id, spi.actors_30d, spi.has_privileged_scope, spi.has_confidential_scope, spi.bound_connector_kind, spi.bound_connector_source_name, spi.connector_enabled, spi.connector_configured, spi.last_success_at, spi.suggested_business_criticality, spi.suggested_data_classification, spi.effective_business_criticality, spi.effective_data_classification,
    CASE spi.bound_connector_kind
      WHEN 'okta' THEN $7::timestamptz
      WHEN 'entra' THEN $8::timestamptz
      WHEN 'google_workspace' THEN $9::timestamptz
      WHEN 'github' THEN $10::timestamptz
      WHEN 'datadog' THEN $11::timestamptz
      WHEN 'aws_identity_center' THEN $12::timestamptz
      ELSE $13::timestamptz
    END AS fresh_after
  FROM saas_app_posture_inputs_v spi
  JOIN scoped_app_ids sai ON sai.saas_app_id = spi.id
  WHERE (
    $14::text = ''
    OR spi.display_name ILIKE ('%' || $14::text || '%')
    OR spi.primary_domain ILIKE ('%' || $14::text || '%')
    OR spi.vendor_name ILIKE ('%' || $14::text || '%')
    OR spi.canonical_key ILIKE ('%' || $14::text || '%')
  )
),
posture_state AS (
  SELECT
    pi.id, pi.canonical_key, pi.display_name, pi.primary_domain, pi.vendor_name, pi.first_seen_at, pi.last_seen_at, pi.created_at, pi.updated_at, pi.owner_identity_id, pi.actors_30d, pi.has_privileged_scope, pi.has_confidential_scope, pi.bound_connector_kind, pi.bound_connector_source_name, pi.connector_enabled, pi.connector_configured, pi.last_success_at, pi.suggested_business_criticality, pi.suggested_data_classification, pi.effective_business_criticality, pi.effective_data_classification, pi.fresh_after,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'unmanaged'
      WHEN NOT pi.connector_configured THEN 'unmanaged'
      WHEN NOT pi.connector_enabled THEN 'unmanaged'
      WHEN pi.last_success_at IS NULL THEN 'unmanaged'
      WHEN pi.last_success_at < pi.fresh_after THEN 'unmanaged'
      ELSE 'managed'
    END AS managed_state,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'no_binding'
      WHEN NOT pi.connector_configured THEN 'connector_not_configured'
      WHEN NOT pi.connector_enabled THEN 'connector_disabled'
      WHEN pi.last_success_at IS NULL THEN 'stale_sync'
      WHEN pi.last_success_at < pi.fresh_after THEN 'stale_sync'
      ELSE 'active_binding_fresh_sync'
    END AS managed_reason
  FROM posture_inputs pi
),
posture_with_risk AS (
  SELECT
    ps.id, ps.canonical_key, ps.display_name, ps.primary_domain, ps.vendor_name, ps.first_seen_at, ps.last_seen_at, ps.created_at, ps.updated_at, ps.owner_identity_id, ps.actors_30d, ps.has_privileged_scope, ps.has_confidential_scope, ps.bound_connector_kind, ps.bound_connector_source_name, ps.connector_enabled, ps.connector_configured, ps.last_success_at, ps.suggested_business_criticality, ps.suggested_data_classification, ps.effective_business_criticality, ps.effective_data_classification, ps.fresh_after, ps.managed_state, ps.managed_reason,
    LEAST(100,
      CASE WHEN ps.managed_state <> 'managed' THEN 45 ELSE 0 END
      + CASE WHEN ps.has_privileged_scope THEN 20 ELSE 0 END
      + CASE WHEN ps.owner_identity_id = 0 THEN 15 ELSE 0 END
      + CASE WHEN ps.actors_30d >= 50 THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_business_criticality IN ('high', 'critical') THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_data_classification IN ('confidential', 'restricted') THEN 5 ELSE 0 END
    )::int AS risk_score
  FROM posture_state ps
),
posture_rows AS (
  SELECT
    pwr.id, pwr.canonical_key, pwr.display_name, pwr.primary_domain, pwr.vendor_name, pwr.first_seen_at, pwr.last_seen_at, pwr.created_at, pwr.updated_at, pwr.owner_identity_id, pwr.actors_30d, pwr.has_privileged_scope, pwr.has_confidential_scope, pwr.bound_connector_kind, pwr.bound_connector_source_name, pwr.connector_enabled, pwr.connector_configured, pwr.last_success_at, pwr.suggested_business_criticality, pwr.suggested_data_classification, pwr.effective_business_criticality, pwr.effective_data_classification, pwr.fresh_after, pwr.managed_state, pwr.managed_reason, pwr.risk_score,
    CASE
      WHEN pwr.risk_score >= 80 THEN 'critical'
      WHEN pwr.risk_score >= 60 THEN 'high'
      WHEN pwr.risk_score >= 30 THEN 'medium'
      ELSE 'low'
    END AS risk_level
  FROM posture_with_risk pwr
)
SELECT count(*)
FROM posture_rows pr
WHERE (
    $1::text = ''
    OR pr.managed_state = $1::text
  )
  AND (
    $2::text = ''
    OR pr.risk_level = $2::text
  )
`

type CountSaaSAppsByFiltersParams struct {
	ManagedState              string             `json:"managed_state"`
	RiskLevel                 string             `json:"risk_level"`
	ConfiguredSourceKinds     []string           `json:"configured_source_kinds"`
	ConfiguredSourceNames     []string           `json:"configured_source_names"`
	SourceKind                string             `json:"source_kind"`
	SourceName                string             `json:"source_name"`
	OktaFreshAfter            pgtype.Timestamptz `json:"okta_fresh_after"`
	EntraFreshAfter           pgtype.Timestamptz `json:"entra_fresh_after"`
	GoogleWorkspaceFreshAfter pgtype.Timestamptz `json:"google_workspace_fresh_after"`
	GithubFreshAfter          pgtype.Timestamptz `json:"github_fresh_after"`
	DatadogFreshAfter         pgtype.Timestamptz `json:"datadog_fresh_after"`
	AwsFreshAfter             pgtype.Timestamptz `json:"aws_fresh_after"`
	DefaultFreshAfter         pgtype.Timestamptz `json:"default_fresh_after"`
	Query                     string             `json:"query"`
}

func (q *Queries) CountSaaSAppsByFilters(ctx context.Context, arg CountSaaSAppsByFiltersParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSaaSAppsByFilters,
		arg.ManagedState,
		arg.RiskLevel,
		arg.ConfiguredSourceKinds,
		arg.ConfiguredSourceNames,
		arg.SourceKind,
		arg.SourceName,
		arg.OktaFreshAfter,
		arg.EntraFreshAfter,
		arg.GoogleWorkspaceFreshAfter,
		arg.GithubFreshAfter,
		arg.DatadogFreshAfter,
		arg.AwsFreshAfter,
		arg.DefaultFreshAfter,
		arg.Query,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countSaaSAppsGroupedByManagedState = `-- name: CountSaaSAppsGroupedByManagedState :many
WITH configured_sources AS (
  SELECT
    k.kind AS source_kind,
    n.name AS source_name
  FROM unnest($1::text[]) WITH ORDINALITY AS k(kind, ord)
  JOIN unnest($2::text[]) WITH ORDINALITY AS n(name, ord) USING (ord)
),
scoped_app_ids AS (
  SELECT DISTINCT sas.saas_app_id
  FROM saas_app_sources sas
  JOIN configured_sources cfg
    ON lower(trim(cfg.source_kind)) = lower(trim(sas.source_kind))
   AND lower(trim(cfg.source_name)) = lower(trim(sas.source_name))
  WHERE sas.expired_at IS NULL
    AND sas.last_observed_run_id IS NOT NULL
),
posture_inputs AS (
  SELECT
    spi.id, spi.canonical_key, spi.display_name, spi.primary_domain, spi.vendor_name, spi.first_seen_at, spi.last_seen_at, spi.created_at, spi.updated_at, spi.owner_identity_id, spi.actors_30d, spi.has_privileged_scope, spi.has_confidential_scope, spi.bound_connector_kind, spi.bound_connector_source_name, spi.connector_enabled, spi.connector_configured, spi.last_success_at, spi.suggested_business_criticality, spi.suggested_data_classification, spi.effective_business_criticality, spi.effective_data_classification,
    CASE spi.bound_connector_kind
      WHEN 'okta' THEN $3::timestamptz
      WHEN 'entra' THEN $4::timestamptz
      WHEN 'google_workspace' THEN $5::timestamptz
      WHEN 'github' THEN $6::timestamptz
      WHEN 'datadog' THEN $7::timestamptz
      WHEN 'aws_identity_center' THEN $8::timestamptz
      ELSE $9::timestamptz
    END AS fresh_after
  FROM saas_app_posture_inputs_v spi
  JOIN scoped_app_ids sai ON sai.saas_app_id = spi.id
),
posture_rows AS (
  SELECT
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'unmanaged'
      WHEN NOT pi.connector_configured THEN 'unmanaged'
      WHEN NOT pi.connector_enabled THEN 'unmanaged'
      WHEN pi.last_success_at IS NULL THEN 'unmanaged'
      WHEN pi.last_success_at < pi.fresh_after THEN 'unmanaged'
      ELSE 'managed'
    END AS managed_state
  FROM posture_inputs pi
)
SELECT pr.managed_state, count(*) AS app_count
FROM posture_rows pr
GROUP BY pr.managed_state
ORDER BY pr.managed_state
`

type CountSaaSAppsGroupedByManagedStateParams struct {
	ConfiguredSourceKinds     []string           `json:"configured_source_kinds"`
	ConfiguredSourceNames     []string           `json:"configured_source_names"`
	OktaFreshAfter            pgtype.Timestamptz `json:"okta_fresh_after"`
	EntraFreshAfter           pgtype.Timestamptz `json:"entra_fresh_after"`
	GoogleWorkspaceFreshAfter pgtype.Timestamptz `json:"google_workspace_fresh_after"`
	GithubFreshAfter          pgtype.Timestamptz `json:"github_fresh_after"`
	DatadogFreshAfter         pgtype.Timestamptz `json:"datadog_fresh_after"`
	AwsFreshAfter             pgtype.Timestamptz `json:"aws_fresh_after"`
	DefaultFreshAfter         pgtype.Timestamptz `json:"default_fresh_after"`
}

type CountSaaSAppsGroupedByManagedStateRow struct {
	ManagedState string `json:"managed_state"`
	AppCount     int64  `json:"app_count"`
}

func (q *Queries) CountSaaSAppsGroupedByManagedState(ctx context.Context, arg CountSaaSAppsGroupedByManagedStateParams) ([]CountSaaSAppsGroupedByManagedStateRow, error) {
	rows, err := q.db.Query(ctx, countSaaSAppsGroupedByManagedState,
		arg.ConfiguredSourceKinds,
		arg.ConfiguredSourceNames,
		arg.OktaFreshAfter,
		arg.EntraFreshAfter,
		arg.GoogleWorkspaceFreshAfter,
		arg.GithubFreshAfter,
		arg.DatadogFreshAfter,
		arg.AwsFreshAfter,
		arg.DefaultFreshAfter,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CountSaaSAppsGroupedByManagedStateRow
	for rows.Next() {
		var i CountSaaSAppsGroupedByManagedStateRow
		if err := rows.Scan(&i.ManagedState, &i.AppCount); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const countSaaSAppsGroupedByRiskLevel = `-- name: CountSaaSAppsGroupedByRiskLevel :many
WITH configured_sources AS (
  SELECT
    k.kind AS source_kind,
    n.name AS source_name
  FROM unnest($1::text[]) WITH ORDINALITY AS k(kind, ord)
  JOIN unnest($2::text[]) WITH ORDINALITY AS n(name, ord) USING (ord)
),
scoped_app_ids AS (
  SELECT DISTINCT sas.saas_app_id
  FROM saas_app_sources sas
  JOIN configured_sources cfg
    ON lower(trim(cfg.source_kind)) = lower(trim(sas.source_kind))
   AND lower(trim(cfg.source_name)) = lower(trim(sas.source_name))
  WHERE sas.expired_at IS NULL
    AND sas.last_observed_run_id IS NOT NULL
),
posture_inputs AS (
  SELECT
    spi.id, spi.canonical_key, spi.display_name, spi.primary_domain, spi.vendor_name, spi.first_seen_at, spi.last_seen_at, spi.created_at, spi.updated_at, spi.owner_identity_id, spi.actors_30d, spi.has_privileged_scope, spi.has_confidential_scope, spi.bound_connector_kind, spi.bound_connector_source_name, spi.connector_enabled, spi.connector_configured, spi.last_success_at, spi.suggested_business_criticality, spi.suggested_data_classification, spi.effective_business_criticality, spi.effective_data_classification,
    CASE spi.bound_connector_kind
      WHEN 'okta' THEN $3::timestamptz
      WHEN 'entra' THEN $4::timestamptz
      WHEN 'google_workspace' THEN $5::timestamptz
      WHEN 'github' THEN $6::timestamptz
      WHEN 'datadog' THEN $7::timestamptz
      WHEN 'aws_identity_center' THEN $8::timestamptz
      ELSE $9::timestamptz
    END AS fresh_after
  FROM saas_app_posture_inputs_v spi
  JOIN scoped_app_ids sai ON sai.saas_app_id = spi.id
),
posture_state AS (
  SELECT
    pi.id, pi.canonical_key, pi.display_name, pi.primary_domain, pi.vendor_name, pi.first_seen_at, pi.last_seen_at, pi.created_at, pi.updated_at, pi.owner_identity_id, pi.actors_30d, pi.has_privileged_scope, pi.has_confidential_scope, pi.bound_connector_kind, pi.bound_connector_source_name, pi.connector_enabled, pi.connector_configured, pi.last_success_at, pi.suggested_business_criticality, pi.suggested_data_classification, pi.effective_business_criticality, pi.effective_data_classification, pi.fresh_after,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'unmanaged'
      WHEN NOT pi.connector_configured THEN 'unmanaged'
      WHEN NOT pi.connector_enabled THEN 'unmanaged'
      WHEN pi.last_success_at IS NULL THEN 'unmanaged'
      WHEN pi.last_success_at < pi.fresh_after THEN 'unmanaged'
      ELSE 'managed'
    END AS managed_state
  FROM posture_inputs pi
),
posture_with_risk AS (
  SELECT
    LEAST(100,
      CASE WHEN ps.managed_state <> 'managed' THEN 45 ELSE 0 END
      + CASE WHEN ps.has_privileged_scope THEN 20 ELSE 0 END
      + CASE WHEN ps.owner_identity_id = 0 THEN 15 ELSE 0 END
      + CASE WHEN ps.actors_30d >= 50 THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_business_criticality IN ('high', 'critical') THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_data_classification IN ('confidential', 'restricted') THEN 5 ELSE 0 END
    )::int AS risk_score
  FROM posture_state ps
),
posture_rows AS (
  SELECT
    CASE
      WHEN pwr.risk_score >= 80 THEN 'critical'
      WHEN pwr.risk_score >= 60 THEN 'high'
      WHEN pwr.risk_score >= 30 THEN 'medium'
      ELSE 'low'
    END AS risk_level
  FROM posture_with_risk pwr
)
SELECT pr.risk_level, count(*) AS app_count
FROM posture_rows pr
GROUP BY pr.risk_level
ORDER BY pr.risk_level
`

type CountSaaSAppsGroupedByRiskLevelParams struct {
	ConfiguredSourceKinds     []string           `json:"configured_source_kinds"`
	ConfiguredSourceNames     []string           `json:"configured_source_names"`
	OktaFreshAfter            pgtype.Timestamptz `json:"okta_fresh_after"`
	EntraFreshAfter           pgtype.Timestamptz `json:"entra_fresh_after"`
	GoogleWorkspaceFreshAfter pgtype.Timestamptz `json:"google_workspace_fresh_after"`
	GithubFreshAfter          pgtype.Timestamptz `json:"github_fresh_after"`
	DatadogFreshAfter         pgtype.Timestamptz `json:"datadog_fresh_after"`
	AwsFreshAfter             pgtype.Timestamptz `json:"aws_fresh_after"`
	DefaultFreshAfter         pgtype.Timestamptz `json:"default_fresh_after"`
}

type CountSaaSAppsGroupedByRiskLevelRow struct {
	RiskLevel string `json:"risk_level"`
	AppCount  int64  `json:"app_count"`
}

func (q *Queries) CountSaaSAppsGroupedByRiskLevel(ctx context.Context, arg CountSaaSAppsGroupedByRiskLevelParams) ([]CountSaaSAppsGroupedByRiskLevelRow, error) {
	rows, err := q.db.Query(ctx, countSaaSAppsGroupedByRiskLevel,
		arg.ConfiguredSourceKinds,
		arg.ConfiguredSourceNames,
		arg.OktaFreshAfter,
		arg.EntraFreshAfter,
		arg.GoogleWorkspaceFreshAfter,
		arg.GithubFreshAfter,
		arg.DatadogFreshAfter,
		arg.AwsFreshAfter,
		arg.DefaultFreshAfter,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CountSaaSAppsGroupedByRiskLevelRow
	for rows.Next() {
		var i CountSaaSAppsGroupedByRiskLevelRow
		if err := rows.Scan(&i.RiskLevel, &i.AppCount); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getSaaSAppByID = `-- name: GetSaaSAppByID :one
WITH posture_inputs AS (
  SELECT
    spi.id, spi.canonical_key, spi.display_name, spi.primary_domain, spi.vendor_name, spi.first_seen_at, spi.last_seen_at, spi.created_at, spi.updated_at, spi.owner_identity_id, spi.actors_30d, spi.has_privileged_scope, spi.has_confidential_scope, spi.bound_connector_kind, spi.bound_connector_source_name, spi.connector_enabled, spi.connector_configured, spi.last_success_at, spi.suggested_business_criticality, spi.suggested_data_classification, spi.effective_business_criticality, spi.effective_data_classification,
    CASE spi.bound_connector_kind
      WHEN 'okta' THEN $1::timestamptz
      WHEN 'entra' THEN $2::timestamptz
      WHEN 'google_workspace' THEN $3::timestamptz
      WHEN 'github' THEN $4::timestamptz
      WHEN 'datadog' THEN $5::timestamptz
      WHEN 'aws_identity_center' THEN $6::timestamptz
      ELSE $7::timestamptz
    END AS fresh_after
  FROM saas_app_posture_inputs_v spi
  WHERE spi.id = $8::bigint
),
posture_state AS (
  SELECT
    pi.id, pi.canonical_key, pi.display_name, pi.primary_domain, pi.vendor_name, pi.first_seen_at, pi.last_seen_at, pi.created_at, pi.updated_at, pi.owner_identity_id, pi.actors_30d, pi.has_privileged_scope, pi.has_confidential_scope, pi.bound_connector_kind, pi.bound_connector_source_name, pi.connector_enabled, pi.connector_configured, pi.last_success_at, pi.suggested_business_criticality, pi.suggested_data_classification, pi.effective_business_criticality, pi.effective_data_classification, pi.fresh_after,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'unmanaged'
      WHEN NOT pi.connector_configured THEN 'unmanaged'
      WHEN NOT pi.connector_enabled THEN 'unmanaged'
      WHEN pi.last_success_at IS NULL THEN 'unmanaged'
      WHEN pi.last_success_at < pi.fresh_after THEN 'unmanaged'
      ELSE 'managed'
    END AS managed_state,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'no_binding'
      WHEN NOT pi.connector_configured THEN 'connector_not_configured'
      WHEN NOT pi.connector_enabled THEN 'connector_disabled'
      WHEN pi.last_success_at IS NULL THEN 'stale_sync'
      WHEN pi.last_success_at < pi.fresh_after THEN 'stale_sync'
      ELSE 'active_binding_fresh_sync'
    END AS managed_reason
  FROM posture_inputs pi
),
posture_with_risk AS (
  SELECT
    ps.id, ps.canonical_key, ps.display_name, ps.primary_domain, ps.vendor_name, ps.first_seen_at, ps.last_seen_at, ps.created_at, ps.updated_at, ps.owner_identity_id, ps.actors_30d, ps.has_privileged_scope, ps.has_confidential_scope, ps.bound_connector_kind, ps.bound_connector_source_name, ps.connector_enabled, ps.connector_configured, ps.last_success_at, ps.suggested_business_criticality, ps.suggested_data_classification, ps.effective_business_criticality, ps.effective_data_classification, ps.fresh_after, ps.managed_state, ps.managed_reason,
    LEAST(100,
      CASE WHEN ps.managed_state <> 'managed' THEN 45 ELSE 0 END
      + CASE WHEN ps.has_privileged_scope THEN 20 ELSE 0 END
      + CASE WHEN ps.owner_identity_id = 0 THEN 15 ELSE 0 END
      + CASE WHEN ps.actors_30d >= 50 THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_business_criticality IN ('high', 'critical') THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_data_classification IN ('confidential', 'restricted') THEN 5 ELSE 0 END
    )::int AS risk_score
  FROM posture_state ps
),
posture_rows AS (
  SELECT
    pwr.id, pwr.canonical_key, pwr.display_name, pwr.primary_domain, pwr.vendor_name, pwr.first_seen_at, pwr.last_seen_at, pwr.created_at, pwr.updated_at, pwr.owner_identity_id, pwr.actors_30d, pwr.has_privileged_scope, pwr.has_confidential_scope, pwr.bound_connector_kind, pwr.bound_connector_source_name, pwr.connector_enabled, pwr.connector_configured, pwr.last_success_at, pwr.suggested_business_criticality, pwr.suggested_data_classification, pwr.effective_business_criticality, pwr.effective_data_classification, pwr.fresh_after, pwr.managed_state, pwr.managed_reason, pwr.risk_score,
    CASE
      WHEN pwr.risk_score >= 80 THEN 'critical'
      WHEN pwr.risk_score >= 60 THEN 'high'
      WHEN pwr.risk_score >= 30 THEN 'medium'
      ELSE 'low'
    END AS risk_level
  FROM posture_with_risk pwr
)
SELECT
  id,
  canonical_key,
  display_name,
  primary_domain,
  vendor_name,
  managed_state,
  managed_reason,
  bound_connector_kind,
  bound_connector_source_name,
  risk_score,
  risk_level,
  suggested_business_criticality,
  suggested_data_classification,
  first_seen_at,
  last_seen_at,
  created_at,
  updated_at
FROM posture_rows
`

type GetSaaSAppByIDParams struct {
	OktaFreshAfter            pgtype.Timestamptz `json:"okta_fresh_after"`
	EntraFreshAfter           pgtype.Timestamptz `json:"entra_fresh_after"`
	GoogleWorkspaceFreshAfter pgtype.Timestamptz `json:"google_workspace_fresh_after"`
	GithubFreshAfter          pgtype.Timestamptz `json:"github_fresh_after"`
	DatadogFreshAfter         pgtype.Timestamptz `json:"datadog_fresh_after"`
	AwsFreshAfter             pgtype.Timestamptz `json:"aws_fresh_after"`
	DefaultFreshAfter         pgtype.Timestamptz `json:"default_fresh_after"`
	ID                        int64              `json:"id"`
}

type GetSaaSAppByIDRow struct {
	ID                           int64              `json:"id"`
	CanonicalKey                 string             `json:"canonical_key"`
	DisplayName                  string             `json:"display_name"`
	PrimaryDomain                string             `json:"primary_domain"`
	VendorName                   string             `json:"vendor_name"`
	ManagedState                 string             `json:"managed_state"`
	ManagedReason                string             `json:"managed_reason"`
	BoundConnectorKind           string             `json:"bound_connector_kind"`
	BoundConnectorSourceName     string             `json:"bound_connector_source_name"`
	RiskScore                    int32              `json:"risk_score"`
	RiskLevel                    string             `json:"risk_level"`
	SuggestedBusinessCriticality string             `json:"suggested_business_criticality"`
	SuggestedDataClassification  string             `json:"suggested_data_classification"`
	FirstSeenAt                  pgtype.Timestamptz `json:"first_seen_at"`
	LastSeenAt                   pgtype.Timestamptz `json:"last_seen_at"`
	CreatedAt                    pgtype.Timestamptz `json:"created_at"`
	UpdatedAt                    pgtype.Timestamptz `json:"updated_at"`
}

func (q *Queries) GetSaaSAppByID(ctx context.Context, arg GetSaaSAppByIDParams) (GetSaaSAppByIDRow, error) {
	row := q.db.QueryRow(ctx, getSaaSAppByID,
		arg.OktaFreshAfter,
		arg.EntraFreshAfter,
		arg.GoogleWorkspaceFreshAfter,
		arg.GithubFreshAfter,
		arg.DatadogFreshAfter,
		arg.AwsFreshAfter,
		arg.DefaultFreshAfter,
		arg.ID,
	)
	var i GetSaaSAppByIDRow
	err := row.Scan(
		&i.ID,
		&i.CanonicalKey,
		&i.DisplayName,
		&i.PrimaryDomain,
		&i.VendorName,
		&i.ManagedState,
		&i.ManagedReason,
		&i.BoundConnectorKind,
		&i.BoundConnectorSourceName,
		&i.RiskScore,
		&i.RiskLevel,
		&i.SuggestedBusinessCriticality,
		&i.SuggestedDataClassification,
		&i.FirstSeenAt,
		&i.LastSeenAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listSaaSAppHotspots = `-- name: ListSaaSAppHotspots :many
WITH configured_sources AS (
  SELECT
    k.kind AS source_kind,
    n.name AS source_name
  FROM unnest($2::text[]) WITH ORDINALITY AS k(kind, ord)
  JOIN unnest($3::text[]) WITH ORDINALITY AS n(name, ord) USING (ord)
),
scoped_app_ids AS (
  SELECT DISTINCT sas.saas_app_id
  FROM saas_app_sources sas
  JOIN configured_sources cfg
    ON lower(trim(cfg.source_kind)) = lower(trim(sas.source_kind))
   AND lower(trim(cfg.source_name)) = lower(trim(sas.source_name))
  WHERE sas.expired_at IS NULL
    AND sas.last_observed_run_id IS NOT NULL
    AND (
      $4::text = ''
      OR lower(trim(sas.source_kind)) = lower(trim($4::text))
    )
    AND (
      $5::text = ''
      OR lower(trim(sas.source_name)) = lower(trim($5::text))
    )
),
posture_inputs AS (
  SELECT
    spi.id, spi.canonical_key, spi.display_name, spi.primary_domain, spi.vendor_name, spi.first_seen_at, spi.last_seen_at, spi.created_at, spi.updated_at, spi.owner_identity_id, spi.actors_30d, spi.has_privileged_scope, spi.has_confidential_scope, spi.bound_connector_kind, spi.bound_connector_source_name, spi.connector_enabled, spi.connector_configured, spi.last_success_at, spi.suggested_business_criticality, spi.suggested_data_classification, spi.effective_business_criticality, spi.effective_data_classification,
    CASE spi.bound_connector_kind
      WHEN 'okta' THEN $6::timestamptz
      WHEN 'entra' THEN $7::timestamptz
      WHEN 'google_workspace' THEN $8::timestamptz
      WHEN 'github' THEN $9::timestamptz
      WHEN 'datadog' THEN $10::timestamptz
      WHEN 'aws_identity_center' THEN $11::timestamptz
      ELSE $12::timestamptz
    END AS fresh_after
  FROM saas_app_posture_inputs_v spi
  JOIN scoped_app_ids sai ON sai.saas_app_id = spi.id
),
posture_state AS (
  SELECT
    pi.id, pi.canonical_key, pi.display_name, pi.primary_domain, pi.vendor_name, pi.first_seen_at, pi.last_seen_at, pi.created_at, pi.updated_at, pi.owner_identity_id, pi.actors_30d, pi.has_privileged_scope, pi.has_confidential_scope, pi.bound_connector_kind, pi.bound_connector_source_name, pi.connector_enabled, pi.connector_configured, pi.last_success_at, pi.suggested_business_criticality, pi.suggested_data_classification, pi.effective_business_criticality, pi.effective_data_classification, pi.fresh_after,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'unmanaged'
      WHEN NOT pi.connector_configured THEN 'unmanaged'
      WHEN NOT pi.connector_enabled THEN 'unmanaged'
      WHEN pi.last_success_at IS NULL THEN 'unmanaged'
      WHEN pi.last_success_at < pi.fresh_after THEN 'unmanaged'
      ELSE 'managed'
    END AS managed_state,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'no_binding'
      WHEN NOT pi.connector_configured THEN 'connector_not_configured'
      WHEN NOT pi.connector_enabled THEN 'connector_disabled'
      WHEN pi.last_success_at IS NULL THEN 'stale_sync'
      WHEN pi.last_success_at < pi.fresh_after THEN 'stale_sync'
      ELSE 'active_binding_fresh_sync'
    END AS managed_reason
  FROM posture_inputs pi
),
posture_with_risk AS (
  SELECT
    ps.id, ps.canonical_key, ps.display_name, ps.primary_domain, ps.vendor_name, ps.first_seen_at, ps.last_seen_at, ps.created_at, ps.updated_at, ps.owner_identity_id, ps.actors_30d, ps.has_privileged_scope, ps.has_confidential_scope, ps.bound_connector_kind, ps.bound_connector_source_name, ps.connector_enabled, ps.connector_configured, ps.last_success_at, ps.suggested_business_criticality, ps.suggested_data_classification, ps.effective_business_criticality, ps.effective_data_classification, ps.fresh_after, ps.managed_state, ps.managed_reason,
    LEAST(100,
      CASE WHEN ps.managed_state <> 'managed' THEN 45 ELSE 0 END
      + CASE WHEN ps.has_privileged_scope THEN 20 ELSE 0 END
      + CASE WHEN ps.owner_identity_id = 0 THEN 15 ELSE 0 END
      + CASE WHEN ps.actors_30d >= 50 THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_business_criticality IN ('high', 'critical') THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_data_classification IN ('confidential', 'restricted') THEN 5 ELSE 0 END
    )::int AS risk_score
  FROM posture_state ps
),
posture_rows AS (
  SELECT
    pwr.id, pwr.canonical_key, pwr.display_name, pwr.primary_domain, pwr.vendor_name, pwr.first_seen_at, pwr.last_seen_at, pwr.created_at, pwr.updated_at, pwr.owner_identity_id, pwr.actors_30d, pwr.has_privileged_scope, pwr.has_confidential_scope, pwr.bound_connector_kind, pwr.bound_connector_source_name, pwr.connector_enabled, pwr.connector_configured, pwr.last_success_at, pwr.suggested_business_criticality, pwr.suggested_data_classification, pwr.effective_business_criticality, pwr.effective_data_classification, pwr.fresh_after, pwr.managed_state, pwr.managed_reason, pwr.risk_score,
    CASE
      WHEN pwr.risk_score >= 80 THEN 'critical'
      WHEN pwr.risk_score >= 60 THEN 'high'
      WHEN pwr.risk_score >= 30 THEN 'medium'
      ELSE 'low'
    END AS risk_level
  FROM posture_with_risk pwr
)
SELECT
  pr.id,
  pr.canonical_key,
  pr.display_name,
  pr.primary_domain,
  pr.vendor_name,
  pr.managed_state,
  pr.managed_reason,
  pr.bound_connector_kind,
  pr.bound_connector_source_name,
  pr.risk_score,
  pr.risk_level,
  pr.suggested_business_criticality,
  pr.suggested_data_classification,
  pr.first_seen_at,
  pr.last_seen_at,
  pr.created_at,
  pr.updated_at,
  COALESCE(owner.display_name, '') AS owner_display_name,
  COALESCE(owner.primary_email, '') AS owner_primary_email,
  pr.actors_30d
FROM posture_rows pr
LEFT JOIN identities owner ON owner.id = NULLIF(pr.owner_identity_id, 0)
WHERE pr.risk_score >= 60
ORDER BY pr.risk_score DESC, pr.last_seen_at DESC, pr.id ASC
LIMIT $1::int
`

type ListSaaSAppHotspotsParams struct {
	LimitRows                 int32              `json:"limit_rows"`
	ConfiguredSourceKinds     []string           `json:"configured_source_kinds"`
	ConfiguredSourceNames     []string           `json:"configured_source_names"`
	SourceKind                string             `json:"source_kind"`
	SourceName                string             `json:"source_name"`
	OktaFreshAfter            pgtype.Timestamptz `json:"okta_fresh_after"`
	EntraFreshAfter           pgtype.Timestamptz `json:"entra_fresh_after"`
	GoogleWorkspaceFreshAfter pgtype.Timestamptz `json:"google_workspace_fresh_after"`
	GithubFreshAfter          pgtype.Timestamptz `json:"github_fresh_after"`
	DatadogFreshAfter         pgtype.Timestamptz `json:"datadog_fresh_after"`
	AwsFreshAfter             pgtype.Timestamptz `json:"aws_fresh_after"`
	DefaultFreshAfter         pgtype.Timestamptz `json:"default_fresh_after"`
}

type ListSaaSAppHotspotsRow struct {
	ID                           int64              `json:"id"`
	CanonicalKey                 string             `json:"canonical_key"`
	DisplayName                  string             `json:"display_name"`
	PrimaryDomain                string             `json:"primary_domain"`
	VendorName                   string             `json:"vendor_name"`
	ManagedState                 string             `json:"managed_state"`
	ManagedReason                string             `json:"managed_reason"`
	BoundConnectorKind           string             `json:"bound_connector_kind"`
	BoundConnectorSourceName     string             `json:"bound_connector_source_name"`
	RiskScore                    int32              `json:"risk_score"`
	RiskLevel                    string             `json:"risk_level"`
	SuggestedBusinessCriticality string             `json:"suggested_business_criticality"`
	SuggestedDataClassification  string             `json:"suggested_data_classification"`
	FirstSeenAt                  pgtype.Timestamptz `json:"first_seen_at"`
	LastSeenAt                   pgtype.Timestamptz `json:"last_seen_at"`
	CreatedAt                    pgtype.Timestamptz `json:"created_at"`
	UpdatedAt                    pgtype.Timestamptz `json:"updated_at"`
	OwnerDisplayName             string             `json:"owner_display_name"`
	OwnerPrimaryEmail            string             `json:"owner_primary_email"`
	Actors30d                    int64              `json:"actors_30d"`
}

func (q *Queries) ListSaaSAppHotspots(ctx context.Context, arg ListSaaSAppHotspotsParams) ([]ListSaaSAppHotspotsRow, error) {
	rows, err := q.db.Query(ctx, listSaaSAppHotspots,
		arg.LimitRows,
		arg.ConfiguredSourceKinds,
		arg.ConfiguredSourceNames,
		arg.SourceKind,
		arg.SourceName,
		arg.OktaFreshAfter,
		arg.EntraFreshAfter,
		arg.GoogleWorkspaceFreshAfter,
		arg.GithubFreshAfter,
		arg.DatadogFreshAfter,
		arg.AwsFreshAfter,
		arg.DefaultFreshAfter,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListSaaSAppHotspotsRow
	for rows.Next() {
		var i ListSaaSAppHotspotsRow
		if err := rows.Scan(
			&i.ID,
			&i.CanonicalKey,
			&i.DisplayName,
			&i.PrimaryDomain,
			&i.VendorName,
			&i.ManagedState,
			&i.ManagedReason,
			&i.BoundConnectorKind,
			&i.BoundConnectorSourceName,
			&i.RiskScore,
			&i.RiskLevel,
			&i.SuggestedBusinessCriticality,
			&i.SuggestedDataClassification,
			&i.FirstSeenAt,
			&i.LastSeenAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.OwnerDisplayName,
			&i.OwnerPrimaryEmail,
			&i.Actors30d,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSaaSAppsPageByFilters = `-- name: ListSaaSAppsPageByFilters :many
WITH configured_sources AS (
  SELECT
    k.kind AS source_kind,
    n.name AS source_name
  FROM unnest($5::text[]) WITH ORDINALITY AS k(kind, ord)
  JOIN unnest($6::text[]) WITH ORDINALITY AS n(name, ord) USING (ord)
),
scoped_app_ids AS (
  SELECT DISTINCT sas.saas_app_id
  FROM saas_app_sources sas
  JOIN configured_sources cfg
    ON lower(trim(cfg.source_kind)) = lower(trim(sas.source_kind))
   AND lower(trim(cfg.source_name)) = lower(trim(sas.source_name))
  WHERE sas.expired_at IS NULL
    AND sas.last_observed_run_id IS NOT NULL
    AND (
      $7::text = ''
      OR lower(trim(sas.source_kind)) = lower(trim($7::text))
    )
    AND (
      $8::text = ''
      OR lower(trim(sas.source_name)) = lower(trim($8::text))
    )
),
posture_inputs AS (
  SELECT
    spi.id, spi.canonical_key, spi.display_name, spi.primary_domain, spi.vendor_name, spi.first_seen_at, spi.last_seen_at, spi.created_at, spi.updated_at, spi.owner_identity_id, spi.actors_30d, spi.has_privileged_scope, spi.has_confidential_scope, spi.bound_connector_kind, spi.bound_connector_source_name, spi.connector_enabled, spi.connector_configured, spi.last_success_at, spi.suggested_business_criticality, spi.suggested_data_classification, spi.effective_business_criticality, spi.effective_data_classification,
    CASE spi.bound_connector_kind
      WHEN 'okta' THEN $9::timestamptz
      WHEN 'entra' THEN $10::timestamptz
      WHEN 'google_workspace' THEN $11::timestamptz
      WHEN 'github' THEN $12::timestamptz
      WHEN 'datadog' THEN $13::timestamptz
      WHEN 'aws_identity_center' THEN $14::timestamptz
      ELSE $15::timestamptz
    END AS fresh_after
  FROM saas_app_posture_inputs_v spi
  JOIN scoped_app_ids sai ON sai.saas_app_id = spi.id
  WHERE (
    $16::text = ''
    OR spi.display_name ILIKE ('%' || $16::text || '%')
    OR spi.primary_domain ILIKE ('%' || $16::text || '%')
    OR spi.vendor_name ILIKE ('%' || $16::text || '%')
    OR spi.canonical_key ILIKE ('%' || $16::text || '%')
  )
),
posture_state AS (
  SELECT
    pi.id, pi.canonical_key, pi.display_name, pi.primary_domain, pi.vendor_name, pi.first_seen_at, pi.last_seen_at, pi.created_at, pi.updated_at, pi.owner_identity_id, pi.actors_30d, pi.has_privileged_scope, pi.has_confidential_scope, pi.bound_connector_kind, pi.bound_connector_source_name, pi.connector_enabled, pi.connector_configured, pi.last_success_at, pi.suggested_business_criticality, pi.suggested_data_classification, pi.effective_business_criticality, pi.effective_data_classification, pi.fresh_after,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'unmanaged'
      WHEN NOT pi.connector_configured THEN 'unmanaged'
      WHEN NOT pi.connector_enabled THEN 'unmanaged'
      WHEN pi.last_success_at IS NULL THEN 'unmanaged'
      WHEN pi.last_success_at < pi.fresh_after THEN 'unmanaged'
      ELSE 'managed'
    END AS managed_state,
    CASE
      WHEN pi.bound_connector_kind = '' OR pi.bound_connector_source_name = '' THEN 'no_binding'
      WHEN NOT pi.connector_configured THEN 'connector_not_configured'
      WHEN NOT pi.connector_enabled THEN 'connector_disabled'
      WHEN pi.last_success_at IS NULL THEN 'stale_sync'
      WHEN pi.last_success_at < pi.fresh_after THEN 'stale_sync'
      ELSE 'active_binding_fresh_sync'
    END AS managed_reason
  FROM posture_inputs pi
),
posture_with_risk AS (
  SELECT
    ps.id, ps.canonical_key, ps.display_name, ps.primary_domain, ps.vendor_name, ps.first_seen_at, ps.last_seen_at, ps.created_at, ps.updated_at, ps.owner_identity_id, ps.actors_30d, ps.has_privileged_scope, ps.has_confidential_scope, ps.bound_connector_kind, ps.bound_connector_source_name, ps.connector_enabled, ps.connector_configured, ps.last_success_at, ps.suggested_business_criticality, ps.suggested_data_classification, ps.effective_business_criticality, ps.effective_data_classification, ps.fresh_after, ps.managed_state, ps.managed_reason,
    LEAST(100,
      CASE WHEN ps.managed_state <> 'managed' THEN 45 ELSE 0 END
      + CASE WHEN ps.has_privileged_scope THEN 20 ELSE 0 END
      + CASE WHEN ps.owner_identity_id = 0 THEN 15 ELSE 0 END
      + CASE WHEN ps.actors_30d >= 50 THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_business_criticality IN ('high', 'critical') THEN 10 ELSE 0 END
      + CASE WHEN ps.managed_state <> 'managed' AND ps.effective_data_classification IN ('confidential', 'restricted') THEN 5 ELSE 0 END
    )::int AS risk_score
  FROM posture_state ps
),
posture_rows AS (
  SELECT
    pwr.id, pwr.canonical_key, pwr.display_name, pwr.primary_domain, pwr.vendor_name, pwr.first_seen_at, pwr.last_seen_at, pwr.created_at, pwr.updated_at, pwr.owner_identity_id, pwr.actors_30d, pwr.has_privileged_scope, pwr.has_confidential_scope, pwr.bound_connector_kind, pwr.bound_connector_source_name, pwr.connector_enabled, pwr.connector_configured, pwr.last_success_at, pwr.suggested_business_criticality, pwr.suggested_data_classification, pwr.effective_business_criticality, pwr.effective_data_classification, pwr.fresh_after, pwr.managed_state, pwr.managed_reason, pwr.risk_score,
    CASE
      WHEN pwr.risk_score >= 80 THEN 'critical'
      WHEN pwr.risk_score >= 60 THEN 'high'
      WHEN pwr.risk_score >= 30 THEN 'medium'
      ELSE 'low'
    END AS risk_level
  FROM posture_with_risk pwr
)
SELECT
  pr.id,
  pr.canonical_key,
  pr.display_name,
  pr.primary_domain,
  pr.vendor_name,
  pr.managed_state,
  pr.managed_reason,
  pr.bound_connector_kind,
  pr.bound_connector_source_name,
  pr.risk_score,
  pr.risk_level,
  pr.suggested_business_criticality,
  pr.suggested_data_classification,
  pr.first_seen_at,
  pr.last_seen_at,
  pr.created_at,
  pr.updated_at,
  COALESCE(owner.display_name, '') AS owner_display_name,
  COALESCE(owner.primary_email, '') AS owner_primary_email,
  pr.actors_30d
FROM posture_rows pr
LEFT JOIN identities owner ON owner.id = NULLIF(pr.owner_identity_id, 0)
WHERE (
    $1::text = ''
    OR pr.managed_state = $1::text
  )
  AND (
    $2::text = ''
    OR pr.risk_level = $2::text
  )
ORDER BY
  pr.risk_score DESC,
  pr.last_seen_at DESC,
  lower(COALESCE(NULLIF(trim(pr.display_name), ''), pr.canonical_key)) ASC,
  pr.id ASC
LIMIT $4::int
OFFSET $3::int
`

type ListSaaSAppsPageByFiltersParams struct {
	ManagedState              string             `json:"managed_state"`
	RiskLevel                 string             `json:"risk_level"`
	PageOffset                int32              `json:"page_offset"`
	PageLimit                 int32              `json:"page_limit"`
	ConfiguredSourceKinds     []string           `json:"configured_source_kinds"`
	ConfiguredSourceNames     []string           `json:"configured_source_names"`
	SourceKind                string             `json:"source_kind"`
	SourceName                string             `json:"source_name"`
	OktaFreshAfter            pgtype.Timestamptz `json:"okta_fresh_after"`
	EntraFreshAfter           pgtype.Timestamptz `json:"entra_fresh_after"`
	GoogleWorkspaceFreshAfter pgtype.Timestamptz `json:"google_workspace_fresh_after"`
	GithubFreshAfter          pgtype.Timestamptz `json:"github_fresh_after"`
	DatadogFreshAfter         pgtype.Timestamptz `json:"datadog_fresh_after"`
	AwsFreshAfter             pgtype.Timestamptz `json:"aws_fresh_after"`
	DefaultFreshAfter         pgtype.Timestamptz `json:"default_fresh_after"`
	Query                     string             `json:"query"`
}

type ListSaaSAppsPageByFiltersRow struct {
	ID                           int64              `json:"id"`
	CanonicalKey                 string             `json:"canonical_key"`
	DisplayName                  string             `json:"display_name"`
	PrimaryDomain                string             `json:"primary_domain"`
	VendorName                   string             `json:"vendor_name"`
	ManagedState                 string             `json:"managed_state"`
	ManagedReason                string             `json:"managed_reason"`
	BoundConnectorKind           string             `json:"bound_connector_kind"`
	BoundConnectorSourceName     string             `json:"bound_connector_source_name"`
	RiskScore                    int32              `json:"risk_score"`
	RiskLevel                    string             `json:"risk_level"`
	SuggestedBusinessCriticality string             `json:"suggested_business_criticality"`
	SuggestedDataClassification  string             `json:"suggested_data_classification"`
	FirstSeenAt                  pgtype.Timestamptz `json:"first_seen_at"`
	LastSeenAt                   pgtype.Timestamptz `json:"last_seen_at"`
	CreatedAt                    pgtype.Timestamptz `json:"created_at"`
	UpdatedAt                    pgtype.Timestamptz `json:"updated_at"`
	OwnerDisplayName             string             `json:"owner_display_name"`
	OwnerPrimaryEmail            string             `json:"owner_primary_email"`
	Actors30d                    int64              `json:"actors_30d"`
}

func (q *Queries) ListSaaSAppsPageByFilters(ctx context.Context, arg ListSaaSAppsPageByFiltersParams) ([]ListSaaSAppsPageByFiltersRow, error) {
	rows, err := q.db.Query(ctx, listSaaSAppsPageByFilters,
		arg.ManagedState,
		arg.RiskLevel,
		arg.PageOffset,
		arg.PageLimit,
		arg.ConfiguredSourceKinds,
		arg.ConfiguredSourceNames,
		arg.SourceKind,
		arg.SourceName,
		arg.OktaFreshAfter,
		arg.EntraFreshAfter,
		arg.GoogleWorkspaceFreshAfter,
		arg.GithubFreshAfter,
		arg.DatadogFreshAfter,
		arg.AwsFreshAfter,
		arg.DefaultFreshAfter,
		arg.Query,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListSaaSAppsPageByFiltersRow
	for rows.Next() {
		var i ListSaaSAppsPageByFiltersRow
		if err := rows.Scan(
			&i.ID,
			&i.CanonicalKey,
			&i.DisplayName,
			&i.PrimaryDomain,
			&i.VendorName,
			&i.ManagedState,
			&i.ManagedReason,
			&i.BoundConnectorKind,
			&i.BoundConnectorSourceName,
			&i.RiskScore,
			&i.RiskLevel,
			&i.SuggestedBusinessCriticality,
			&i.SuggestedDataClassification,
			&i.FirstSeenAt,
			&i.LastSeenAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.OwnerDisplayName,
			&i.OwnerPrimaryEmail,
			&i.Actors30d,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertSaaSAppsBulk = `-- name: UpsertSaaSAppsBulk :execrows
WITH input AS (
  SELECT
    i,
    ($1::text[])[i] AS canonical_key,
    ($2::text[])[i] AS display_name,
    ($3::text[])[i] AS primary_domain,
    ($4::text[])[i] AS vendor_name,
    ($5::timestamptz[])[i] AS first_seen_at,
    ($6::timestamptz[])[i] AS last_seen_at
  FROM generate_subscripts($1::text[], 1) AS s(i)
),
dedup AS (
  SELECT DISTINCT ON (canonical_key)
    canonical_key,
    display_name,
    primary_domain,
    vendor_name,
    first_seen_at,
    last_seen_at
  FROM input
  ORDER BY canonical_key, i DESC
)
INSERT INTO saas_apps (
  canonical_key,
  display_name,
  primary_domain,
  vendor_name,
  first_seen_at,
  last_seen_at,
  updated_at
)
SELECT
  d.canonical_key,
  d.display_name,
  d.primary_domain,
  d.vendor_name,
  COALESCE(d.first_seen_at, now()),
  COALESCE(d.last_seen_at, now()),
  now()
FROM dedup d
ON CONFLICT (canonical_key) DO UPDATE SET
  display_name = CASE
    WHEN trim(EXCLUDED.display_name) <> '' THEN EXCLUDED.display_name
    ELSE saas_apps.display_name
  END,
  primary_domain = CASE
    WHEN trim(EXCLUDED.primary_domain) <> '' THEN EXCLUDED.primary_domain
    ELSE saas_apps.primary_domain
  END,
  vendor_name = CASE
    WHEN trim(EXCLUDED.vendor_name) <> '' THEN EXCLUDED.vendor_name
    ELSE saas_apps.vendor_name
  END,
  first_seen_at = LEAST(saas_apps.first_seen_at, COALESCE(EXCLUDED.first_seen_at, saas_apps.first_seen_at)),
  last_seen_at = GREATEST(saas_apps.last_seen_at, COALESCE(EXCLUDED.last_seen_at, saas_apps.last_seen_at)),
  updated_at = now()
`

type UpsertSaaSAppsBulkParams struct {
	CanonicalKeys  []string             `json:"canonical_keys"`
	DisplayNames   []string             `json:"display_names"`
	PrimaryDomains []string             `json:"primary_domains"`
	VendorNames    []string             `json:"vendor_names"`
	FirstSeenAts   []pgtype.Timestamptz `json:"first_seen_ats"`
	LastSeenAts    []pgtype.Timestamptz `json:"last_seen_ats"`
}

func (q *Queries) UpsertSaaSAppsBulk(ctx context.Context, arg UpsertSaaSAppsBulkParams) (int64, error) {
	result, err := q.db.Exec(ctx, upsertSaaSAppsBulk,
		arg.CanonicalKeys,
		arg.DisplayNames,
		arg.PrimaryDomains,
		arg.VendorNames,
		arg.FirstSeenAts,
		arg.LastSeenAts,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
