// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: source_accounts.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countGitHubUsersBySourceAndQuery = `-- name: CountGitHubUsersBySourceAndQuery :one
SELECT count(*)
FROM accounts au
WHERE
  au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    au.entity_category = 'user'
    OR (
      au.entity_category = 'unknown'
      AND lower(trim(au.external_id)) NOT LIKE 'team:%'
    )
  )
  AND (
    $3::text = ''
    OR au.external_id ILIKE ('%' || $3::text || '%')
    OR au.email ILIKE ('%' || $3::text || '%')
    OR au.display_name ILIKE ('%' || $3::text || '%')
  )
`

type CountGitHubUsersBySourceAndQueryParams struct {
	SourceKind string `json:"source_kind"`
	SourceName string `json:"source_name"`
	Query      string `json:"query"`
}

func (q *Queries) CountGitHubUsersBySourceAndQuery(ctx context.Context, arg CountGitHubUsersBySourceAndQueryParams) (int64, error) {
	row := q.db.QueryRow(ctx, countGitHubUsersBySourceAndQuery, arg.SourceKind, arg.SourceName, arg.Query)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countLinkedSourceAccountsBySource = `-- name: CountLinkedSourceAccountsBySource :one
WITH authoritative_identities AS (
  SELECT DISTINCT ia.identity_id
  FROM identity_accounts ia
  JOIN accounts anchor ON anchor.id = ia.account_id
  JOIN identity_source_settings iss
    ON iss.source_kind = anchor.source_kind
   AND iss.source_name = anchor.source_name
   AND iss.is_authoritative
  WHERE anchor.expired_at IS NULL
    AND anchor.last_observed_run_id IS NOT NULL
)
SELECT count(*)
FROM accounts au
JOIN identity_accounts ia ON ia.account_id = au.id
JOIN authoritative_identities ai ON ai.identity_id = ia.identity_id
WHERE au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
`

type CountLinkedSourceAccountsBySourceParams struct {
	SourceKind string `json:"source_kind"`
	SourceName string `json:"source_name"`
}

func (q *Queries) CountLinkedSourceAccountsBySource(ctx context.Context, arg CountLinkedSourceAccountsBySourceParams) (int64, error) {
	row := q.db.QueryRow(ctx, countLinkedSourceAccountsBySource, arg.SourceKind, arg.SourceName)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countSourceAccountsBySource = `-- name: CountSourceAccountsBySource :one
SELECT count(*)
FROM accounts
WHERE source_kind = $1
  AND source_name = $2
  AND expired_at IS NULL
  AND last_observed_run_id IS NOT NULL
`

type CountSourceAccountsBySourceParams struct {
	SourceKind string `json:"source_kind"`
	SourceName string `json:"source_name"`
}

func (q *Queries) CountSourceAccountsBySource(ctx context.Context, arg CountSourceAccountsBySourceParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSourceAccountsBySource, arg.SourceKind, arg.SourceName)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countSourceAccountsBySourceAndQuery = `-- name: CountSourceAccountsBySourceAndQuery :one
SELECT count(*)
FROM accounts au
WHERE
  au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    $3::text = ''
    OR au.entity_category = $3::text
  )
  AND (
    $4::text = ''
    OR au.external_id ILIKE ('%' || $4::text || '%')
    OR au.email ILIKE ('%' || $4::text || '%')
    OR au.display_name ILIKE ('%' || $4::text || '%')
  )
`

type CountSourceAccountsBySourceAndQueryParams struct {
	SourceKind     string `json:"source_kind"`
	SourceName     string `json:"source_name"`
	EntityCategory string `json:"entity_category"`
	Query          string `json:"query"`
}

func (q *Queries) CountSourceAccountsBySourceAndQuery(ctx context.Context, arg CountSourceAccountsBySourceAndQueryParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSourceAccountsBySourceAndQuery,
		arg.SourceKind,
		arg.SourceName,
		arg.EntityCategory,
		arg.Query,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countSourceAccountsBySourceAndQueryAndState = `-- name: CountSourceAccountsBySourceAndQueryAndState :one
SELECT count(*)
FROM accounts au
WHERE
  au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    $3::text = ''
    OR au.entity_category = $3::text
  )
  AND (
    $4::text = ''
    OR au.external_id ILIKE ('%' || $4::text || '%')
    OR au.email ILIKE ('%' || $4::text || '%')
    OR au.display_name ILIKE ('%' || $4::text || '%')
  )
  AND (
    $5::text = ''
    OR (
      $5::text = 'active'
      AND lower(COALESCE(NULLIF(trim(au.status), ''), NULLIF(trim(au.raw_json->>'status'), ''), '')) = 'active'
    )
    OR (
      $5::text = 'inactive'
      AND lower(COALESCE(NULLIF(trim(au.status), ''), NULLIF(trim(au.raw_json->>'status'), ''), '')) <> 'active'
    )
  )
`

type CountSourceAccountsBySourceAndQueryAndStateParams struct {
	SourceKind     string `json:"source_kind"`
	SourceName     string `json:"source_name"`
	EntityCategory string `json:"entity_category"`
	Query          string `json:"query"`
	State          string `json:"state"`
}

func (q *Queries) CountSourceAccountsBySourceAndQueryAndState(ctx context.Context, arg CountSourceAccountsBySourceAndQueryAndStateParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSourceAccountsBySourceAndQueryAndState,
		arg.SourceKind,
		arg.SourceName,
		arg.EntityCategory,
		arg.Query,
		arg.State,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countUnlinkedSourceAccountsBySource = `-- name: CountUnlinkedSourceAccountsBySource :one
WITH authoritative_identities AS (
  SELECT DISTINCT ia.identity_id
  FROM identity_accounts ia
  JOIN accounts anchor ON anchor.id = ia.account_id
  JOIN identity_source_settings iss
    ON iss.source_kind = anchor.source_kind
   AND iss.source_name = anchor.source_name
   AND iss.is_authoritative
  WHERE anchor.expired_at IS NULL
    AND anchor.last_observed_run_id IS NOT NULL
)
SELECT count(*)
FROM accounts au
LEFT JOIN identity_accounts ia ON ia.account_id = au.id
LEFT JOIN authoritative_identities ai ON ai.identity_id = ia.identity_id
WHERE au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    ia.identity_id IS NULL
    OR ai.identity_id IS NULL
  )
`

type CountUnlinkedSourceAccountsBySourceParams struct {
	SourceKind string `json:"source_kind"`
	SourceName string `json:"source_name"`
}

func (q *Queries) CountUnlinkedSourceAccountsBySource(ctx context.Context, arg CountUnlinkedSourceAccountsBySourceParams) (int64, error) {
	row := q.db.QueryRow(ctx, countUnlinkedSourceAccountsBySource, arg.SourceKind, arg.SourceName)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countUnlinkedSourceAccountsBySourceAndQuery = `-- name: CountUnlinkedSourceAccountsBySourceAndQuery :one
WITH authoritative_identities AS (
  SELECT DISTINCT ia.identity_id
  FROM identity_accounts ia
  JOIN accounts anchor ON anchor.id = ia.account_id
  JOIN identity_source_settings iss
    ON iss.source_kind = anchor.source_kind
   AND iss.source_name = anchor.source_name
   AND iss.is_authoritative
  WHERE anchor.expired_at IS NULL
    AND anchor.last_observed_run_id IS NOT NULL
)
SELECT count(*)
FROM accounts au
LEFT JOIN identity_accounts ia ON ia.account_id = au.id
LEFT JOIN authoritative_identities ai ON ai.identity_id = ia.identity_id
WHERE
  au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    $3::text = ''
    OR au.entity_category = $3::text
  )
  AND (
    ia.identity_id IS NULL
    OR ai.identity_id IS NULL
  )
  AND (
    $4::text = ''
    OR au.external_id ILIKE ('%' || $4::text || '%')
    OR au.email ILIKE ('%' || $4::text || '%')
    OR au.display_name ILIKE ('%' || $4::text || '%')
  )
`

type CountUnlinkedSourceAccountsBySourceAndQueryParams struct {
	SourceKind     string `json:"source_kind"`
	SourceName     string `json:"source_name"`
	EntityCategory string `json:"entity_category"`
	Query          string `json:"query"`
}

func (q *Queries) CountUnlinkedSourceAccountsBySourceAndQuery(ctx context.Context, arg CountUnlinkedSourceAccountsBySourceAndQueryParams) (int64, error) {
	row := q.db.QueryRow(ctx, countUnlinkedSourceAccountsBySourceAndQuery,
		arg.SourceKind,
		arg.SourceName,
		arg.EntityCategory,
		arg.Query,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getSourceAccount = `-- name: GetSourceAccount :one
SELECT id, source_kind, source_name, external_id, email, display_name, raw_json, created_at, updated_at, last_login_at, last_login_ip, last_login_region, seen_in_run_id, seen_at, last_observed_run_id, last_observed_at, expired_at, expired_run_id, status, account_kind, entity_category
FROM accounts
WHERE id = $1
  AND expired_at IS NULL
  AND last_observed_run_id IS NOT NULL
`

func (q *Queries) GetSourceAccount(ctx context.Context, id int64) (Account, error) {
	row := q.db.QueryRow(ctx, getSourceAccount, id)
	var i Account
	err := row.Scan(
		&i.ID,
		&i.SourceKind,
		&i.SourceName,
		&i.ExternalID,
		&i.Email,
		&i.DisplayName,
		&i.RawJson,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.LastLoginAt,
		&i.LastLoginIp,
		&i.LastLoginRegion,
		&i.SeenInRunID,
		&i.SeenAt,
		&i.LastObservedRunID,
		&i.LastObservedAt,
		&i.ExpiredAt,
		&i.ExpiredRunID,
		&i.Status,
		&i.AccountKind,
		&i.EntityCategory,
	)
	return i, err
}

const listGitHubUsersPageBySourceAndQuery = `-- name: ListGitHubUsersPageBySourceAndQuery :many
SELECT
  au.id, au.source_kind, au.source_name, au.external_id, au.email, au.display_name, au.raw_json, au.created_at, au.updated_at, au.last_login_at, au.last_login_ip, au.last_login_region, au.seen_in_run_id, au.seen_at, au.last_observed_run_id, au.last_observed_at, au.expired_at, au.expired_run_id, au.status, au.account_kind, au.entity_category,
  COALESCE(ia.identity_id, 0) AS identity_id
FROM accounts au
LEFT JOIN identity_accounts ia ON ia.account_id = au.id
WHERE
  au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    au.entity_category = 'user'
    OR (
      au.entity_category = 'unknown'
      AND lower(trim(au.external_id)) NOT LIKE 'team:%'
    )
  )
  AND (
    $3::text = ''
    OR au.external_id ILIKE ('%' || $3::text || '%')
    OR au.email ILIKE ('%' || $3::text || '%')
    OR au.display_name ILIKE ('%' || $3::text || '%')
  )
ORDER BY au.id DESC
LIMIT $5::int
OFFSET $4::int
`

type ListGitHubUsersPageBySourceAndQueryParams struct {
	SourceKind string `json:"source_kind"`
	SourceName string `json:"source_name"`
	Query      string `json:"query"`
	PageOffset int32  `json:"page_offset"`
	PageLimit  int32  `json:"page_limit"`
}

type ListGitHubUsersPageBySourceAndQueryRow struct {
	ID                int64              `json:"id"`
	SourceKind        string             `json:"source_kind"`
	SourceName        string             `json:"source_name"`
	ExternalID        string             `json:"external_id"`
	Email             string             `json:"email"`
	DisplayName       string             `json:"display_name"`
	RawJson           []byte             `json:"raw_json"`
	CreatedAt         pgtype.Timestamptz `json:"created_at"`
	UpdatedAt         pgtype.Timestamptz `json:"updated_at"`
	LastLoginAt       pgtype.Timestamptz `json:"last_login_at"`
	LastLoginIp       string             `json:"last_login_ip"`
	LastLoginRegion   string             `json:"last_login_region"`
	SeenInRunID       pgtype.Int8        `json:"seen_in_run_id"`
	SeenAt            pgtype.Timestamptz `json:"seen_at"`
	LastObservedRunID pgtype.Int8        `json:"last_observed_run_id"`
	LastObservedAt    pgtype.Timestamptz `json:"last_observed_at"`
	ExpiredAt         pgtype.Timestamptz `json:"expired_at"`
	ExpiredRunID      pgtype.Int8        `json:"expired_run_id"`
	Status            string             `json:"status"`
	AccountKind       string             `json:"account_kind"`
	EntityCategory    string             `json:"entity_category"`
	IdentityID        int64              `json:"identity_id"`
}

func (q *Queries) ListGitHubUsersPageBySourceAndQuery(ctx context.Context, arg ListGitHubUsersPageBySourceAndQueryParams) ([]ListGitHubUsersPageBySourceAndQueryRow, error) {
	rows, err := q.db.Query(ctx, listGitHubUsersPageBySourceAndQuery,
		arg.SourceKind,
		arg.SourceName,
		arg.Query,
		arg.PageOffset,
		arg.PageLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListGitHubUsersPageBySourceAndQueryRow
	for rows.Next() {
		var i ListGitHubUsersPageBySourceAndQueryRow
		if err := rows.Scan(
			&i.ID,
			&i.SourceKind,
			&i.SourceName,
			&i.ExternalID,
			&i.Email,
			&i.DisplayName,
			&i.RawJson,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastLoginAt,
			&i.LastLoginIp,
			&i.LastLoginRegion,
			&i.SeenInRunID,
			&i.SeenAt,
			&i.LastObservedRunID,
			&i.LastObservedAt,
			&i.ExpiredAt,
			&i.ExpiredRunID,
			&i.Status,
			&i.AccountKind,
			&i.EntityCategory,
			&i.IdentityID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSourceAccountsPageBySourceAndQuery = `-- name: ListSourceAccountsPageBySourceAndQuery :many
SELECT
  au.id, au.source_kind, au.source_name, au.external_id, au.email, au.display_name, au.raw_json, au.created_at, au.updated_at, au.last_login_at, au.last_login_ip, au.last_login_region, au.seen_in_run_id, au.seen_at, au.last_observed_run_id, au.last_observed_at, au.expired_at, au.expired_run_id, au.status, au.account_kind, au.entity_category,
  COALESCE(ia.identity_id, 0) AS identity_id
FROM accounts au
LEFT JOIN identity_accounts ia ON ia.account_id = au.id
WHERE
  au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    $3::text = ''
    OR au.entity_category = $3::text
  )
  AND (
    $4::text = ''
    OR au.external_id ILIKE ('%' || $4::text || '%')
    OR au.email ILIKE ('%' || $4::text || '%')
    OR au.display_name ILIKE ('%' || $4::text || '%')
  )
ORDER BY au.id DESC
LIMIT $6::int
OFFSET $5::int
`

type ListSourceAccountsPageBySourceAndQueryParams struct {
	SourceKind     string `json:"source_kind"`
	SourceName     string `json:"source_name"`
	EntityCategory string `json:"entity_category"`
	Query          string `json:"query"`
	PageOffset     int32  `json:"page_offset"`
	PageLimit      int32  `json:"page_limit"`
}

type ListSourceAccountsPageBySourceAndQueryRow struct {
	ID                int64              `json:"id"`
	SourceKind        string             `json:"source_kind"`
	SourceName        string             `json:"source_name"`
	ExternalID        string             `json:"external_id"`
	Email             string             `json:"email"`
	DisplayName       string             `json:"display_name"`
	RawJson           []byte             `json:"raw_json"`
	CreatedAt         pgtype.Timestamptz `json:"created_at"`
	UpdatedAt         pgtype.Timestamptz `json:"updated_at"`
	LastLoginAt       pgtype.Timestamptz `json:"last_login_at"`
	LastLoginIp       string             `json:"last_login_ip"`
	LastLoginRegion   string             `json:"last_login_region"`
	SeenInRunID       pgtype.Int8        `json:"seen_in_run_id"`
	SeenAt            pgtype.Timestamptz `json:"seen_at"`
	LastObservedRunID pgtype.Int8        `json:"last_observed_run_id"`
	LastObservedAt    pgtype.Timestamptz `json:"last_observed_at"`
	ExpiredAt         pgtype.Timestamptz `json:"expired_at"`
	ExpiredRunID      pgtype.Int8        `json:"expired_run_id"`
	Status            string             `json:"status"`
	AccountKind       string             `json:"account_kind"`
	EntityCategory    string             `json:"entity_category"`
	IdentityID        int64              `json:"identity_id"`
}

func (q *Queries) ListSourceAccountsPageBySourceAndQuery(ctx context.Context, arg ListSourceAccountsPageBySourceAndQueryParams) ([]ListSourceAccountsPageBySourceAndQueryRow, error) {
	rows, err := q.db.Query(ctx, listSourceAccountsPageBySourceAndQuery,
		arg.SourceKind,
		arg.SourceName,
		arg.EntityCategory,
		arg.Query,
		arg.PageOffset,
		arg.PageLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListSourceAccountsPageBySourceAndQueryRow
	for rows.Next() {
		var i ListSourceAccountsPageBySourceAndQueryRow
		if err := rows.Scan(
			&i.ID,
			&i.SourceKind,
			&i.SourceName,
			&i.ExternalID,
			&i.Email,
			&i.DisplayName,
			&i.RawJson,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastLoginAt,
			&i.LastLoginIp,
			&i.LastLoginRegion,
			&i.SeenInRunID,
			&i.SeenAt,
			&i.LastObservedRunID,
			&i.LastObservedAt,
			&i.ExpiredAt,
			&i.ExpiredRunID,
			&i.Status,
			&i.AccountKind,
			&i.EntityCategory,
			&i.IdentityID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSourceAccountsPageBySourceAndQueryAndState = `-- name: ListSourceAccountsPageBySourceAndQueryAndState :many
SELECT au.id, au.source_kind, au.source_name, au.external_id, au.email, au.display_name, au.raw_json, au.created_at, au.updated_at, au.last_login_at, au.last_login_ip, au.last_login_region, au.seen_in_run_id, au.seen_at, au.last_observed_run_id, au.last_observed_at, au.expired_at, au.expired_run_id, au.status, au.account_kind, au.entity_category
FROM accounts au
WHERE
  au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    $3::text = ''
    OR au.entity_category = $3::text
  )
  AND (
    $4::text = ''
    OR au.external_id ILIKE ('%' || $4::text || '%')
    OR au.email ILIKE ('%' || $4::text || '%')
    OR au.display_name ILIKE ('%' || $4::text || '%')
  )
  AND (
    $5::text = ''
    OR (
      $5::text = 'active'
      AND lower(COALESCE(NULLIF(trim(au.status), ''), NULLIF(trim(au.raw_json->>'status'), ''), '')) = 'active'
    )
    OR (
      $5::text = 'inactive'
      AND lower(COALESCE(NULLIF(trim(au.status), ''), NULLIF(trim(au.raw_json->>'status'), ''), '')) <> 'active'
    )
  )
ORDER BY au.id DESC
LIMIT $7::int
OFFSET $6::int
`

type ListSourceAccountsPageBySourceAndQueryAndStateParams struct {
	SourceKind     string `json:"source_kind"`
	SourceName     string `json:"source_name"`
	EntityCategory string `json:"entity_category"`
	Query          string `json:"query"`
	State          string `json:"state"`
	PageOffset     int32  `json:"page_offset"`
	PageLimit      int32  `json:"page_limit"`
}

func (q *Queries) ListSourceAccountsPageBySourceAndQueryAndState(ctx context.Context, arg ListSourceAccountsPageBySourceAndQueryAndStateParams) ([]Account, error) {
	rows, err := q.db.Query(ctx, listSourceAccountsPageBySourceAndQueryAndState,
		arg.SourceKind,
		arg.SourceName,
		arg.EntityCategory,
		arg.Query,
		arg.State,
		arg.PageOffset,
		arg.PageLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Account
	for rows.Next() {
		var i Account
		if err := rows.Scan(
			&i.ID,
			&i.SourceKind,
			&i.SourceName,
			&i.ExternalID,
			&i.Email,
			&i.DisplayName,
			&i.RawJson,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastLoginAt,
			&i.LastLoginIp,
			&i.LastLoginRegion,
			&i.SeenInRunID,
			&i.SeenAt,
			&i.LastObservedRunID,
			&i.LastObservedAt,
			&i.ExpiredAt,
			&i.ExpiredRunID,
			&i.Status,
			&i.AccountKind,
			&i.EntityCategory,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSourceAccountsPageBySourceAndQueryWithEntitlementCounts = `-- name: ListSourceAccountsPageBySourceAndQueryWithEntitlementCounts :many
WITH page AS (
  SELECT
    au.id, au.source_kind, au.source_name, au.external_id, au.email, au.display_name, au.raw_json, au.created_at, au.updated_at, au.last_login_at, au.last_login_ip, au.last_login_region, au.seen_in_run_id, au.seen_at, au.last_observed_run_id, au.last_observed_at, au.expired_at, au.expired_run_id, au.status, au.account_kind, au.entity_category,
    COALESCE(ia.identity_id, 0) AS identity_id
  FROM accounts au
  LEFT JOIN identity_accounts ia ON ia.account_id = au.id
  WHERE
    au.source_kind = $1
    AND au.source_name = $2
    AND au.expired_at IS NULL
    AND au.last_observed_run_id IS NOT NULL
    AND (
      $3::text = ''
      OR au.entity_category = $3::text
    )
    AND (
      $4::text = ''
      OR au.external_id ILIKE ('%' || $4::text || '%')
      OR au.email ILIKE ('%' || $4::text || '%')
      OR au.display_name ILIKE ('%' || $4::text || '%')
    )
  ORDER BY au.id DESC
  LIMIT $6::int
  OFFSET $5::int
),
requested_accounts AS (
  SELECT DISTINCT page.id
  FROM page
),
entitlement_counts AS (
  SELECT
    e.app_user_id AS account_id,
    count(DISTINCT NULLIF(trim(e.resource), '')) FILTER (
      WHERE $7::text <> ''
        AND e.kind = $7::text
    )::bigint AS distinct_resource_count_1,
    count(DISTINCT NULLIF(trim(e.resource), '')) FILTER (
      WHERE $8::text <> ''
        AND e.kind = $8::text
    )::bigint AS distinct_resource_count_2,
    count(*) FILTER (
      WHERE $9::text <> ''
        AND e.kind = $9::text
        AND NULLIF(trim(e.resource), '') IS NOT NULL
    )::bigint AS entitlement_count_1
  FROM entitlements e
  JOIN requested_accounts ra ON ra.id = e.app_user_id
  WHERE e.expired_at IS NULL
    AND e.last_observed_run_id IS NOT NULL
  GROUP BY e.app_user_id
)
SELECT
  page.id, page.source_kind, page.source_name, page.external_id, page.email, page.display_name, page.raw_json, page.created_at, page.updated_at, page.last_login_at, page.last_login_ip, page.last_login_region, page.seen_in_run_id, page.seen_at, page.last_observed_run_id, page.last_observed_at, page.expired_at, page.expired_run_id, page.status, page.account_kind, page.entity_category, page.identity_id,
  COALESCE(entitlement_counts.distinct_resource_count_1, 0)::bigint AS distinct_resource_count_1,
  COALESCE(entitlement_counts.distinct_resource_count_2, 0)::bigint AS distinct_resource_count_2,
  COALESCE(entitlement_counts.entitlement_count_1, 0)::bigint AS entitlement_count_1
FROM page
LEFT JOIN entitlement_counts ON entitlement_counts.account_id = page.id
ORDER BY page.id DESC
`

type ListSourceAccountsPageBySourceAndQueryWithEntitlementCountsParams struct {
	SourceKind            string `json:"source_kind"`
	SourceName            string `json:"source_name"`
	EntityCategory        string `json:"entity_category"`
	Query                 string `json:"query"`
	PageOffset            int32  `json:"page_offset"`
	PageLimit             int32  `json:"page_limit"`
	DistinctResourceKind1 string `json:"distinct_resource_kind_1"`
	DistinctResourceKind2 string `json:"distinct_resource_kind_2"`
	EntitlementKind1      string `json:"entitlement_kind_1"`
}

type ListSourceAccountsPageBySourceAndQueryWithEntitlementCountsRow struct {
	ID                     int64              `json:"id"`
	SourceKind             string             `json:"source_kind"`
	SourceName             string             `json:"source_name"`
	ExternalID             string             `json:"external_id"`
	Email                  string             `json:"email"`
	DisplayName            string             `json:"display_name"`
	RawJson                []byte             `json:"raw_json"`
	CreatedAt              pgtype.Timestamptz `json:"created_at"`
	UpdatedAt              pgtype.Timestamptz `json:"updated_at"`
	LastLoginAt            pgtype.Timestamptz `json:"last_login_at"`
	LastLoginIp            string             `json:"last_login_ip"`
	LastLoginRegion        string             `json:"last_login_region"`
	SeenInRunID            pgtype.Int8        `json:"seen_in_run_id"`
	SeenAt                 pgtype.Timestamptz `json:"seen_at"`
	LastObservedRunID      pgtype.Int8        `json:"last_observed_run_id"`
	LastObservedAt         pgtype.Timestamptz `json:"last_observed_at"`
	ExpiredAt              pgtype.Timestamptz `json:"expired_at"`
	ExpiredRunID           pgtype.Int8        `json:"expired_run_id"`
	Status                 string             `json:"status"`
	AccountKind            string             `json:"account_kind"`
	EntityCategory         string             `json:"entity_category"`
	IdentityID             int64              `json:"identity_id"`
	DistinctResourceCount1 int64              `json:"distinct_resource_count_1"`
	DistinctResourceCount2 int64              `json:"distinct_resource_count_2"`
	EntitlementCount1      int64              `json:"entitlement_count_1"`
}

func (q *Queries) ListSourceAccountsPageBySourceAndQueryWithEntitlementCounts(ctx context.Context, arg ListSourceAccountsPageBySourceAndQueryWithEntitlementCountsParams) ([]ListSourceAccountsPageBySourceAndQueryWithEntitlementCountsRow, error) {
	rows, err := q.db.Query(ctx, listSourceAccountsPageBySourceAndQueryWithEntitlementCounts,
		arg.SourceKind,
		arg.SourceName,
		arg.EntityCategory,
		arg.Query,
		arg.PageOffset,
		arg.PageLimit,
		arg.DistinctResourceKind1,
		arg.DistinctResourceKind2,
		arg.EntitlementKind1,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListSourceAccountsPageBySourceAndQueryWithEntitlementCountsRow
	for rows.Next() {
		var i ListSourceAccountsPageBySourceAndQueryWithEntitlementCountsRow
		if err := rows.Scan(
			&i.ID,
			&i.SourceKind,
			&i.SourceName,
			&i.ExternalID,
			&i.Email,
			&i.DisplayName,
			&i.RawJson,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastLoginAt,
			&i.LastLoginIp,
			&i.LastLoginRegion,
			&i.SeenInRunID,
			&i.SeenAt,
			&i.LastObservedRunID,
			&i.LastObservedAt,
			&i.ExpiredAt,
			&i.ExpiredRunID,
			&i.Status,
			&i.AccountKind,
			&i.EntityCategory,
			&i.IdentityID,
			&i.DistinctResourceCount1,
			&i.DistinctResourceCount2,
			&i.EntitlementCount1,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listUnlinkedSourceAccountsPageBySourceAndQuery = `-- name: ListUnlinkedSourceAccountsPageBySourceAndQuery :many
WITH authoritative_identities AS (
  SELECT DISTINCT ia.identity_id
  FROM identity_accounts ia
  JOIN accounts anchor ON anchor.id = ia.account_id
  JOIN identity_source_settings iss
    ON iss.source_kind = anchor.source_kind
   AND iss.source_name = anchor.source_name
   AND iss.is_authoritative
  WHERE anchor.expired_at IS NULL
    AND anchor.last_observed_run_id IS NOT NULL
)
SELECT au.id, au.source_kind, au.source_name, au.external_id, au.email, au.display_name, au.raw_json, au.created_at, au.updated_at, au.last_login_at, au.last_login_ip, au.last_login_region, au.seen_in_run_id, au.seen_at, au.last_observed_run_id, au.last_observed_at, au.expired_at, au.expired_run_id, au.status, au.account_kind, au.entity_category
FROM accounts au
LEFT JOIN identity_accounts ia ON ia.account_id = au.id
LEFT JOIN authoritative_identities ai ON ai.identity_id = ia.identity_id
WHERE
  au.source_kind = $1
  AND au.source_name = $2
  AND au.expired_at IS NULL
  AND au.last_observed_run_id IS NOT NULL
  AND (
    $3::text = ''
    OR au.entity_category = $3::text
  )
  AND (
    ia.identity_id IS NULL
    OR ai.identity_id IS NULL
  )
  AND (
    $4::text = ''
    OR au.external_id ILIKE ('%' || $4::text || '%')
    OR au.email ILIKE ('%' || $4::text || '%')
    OR au.display_name ILIKE ('%' || $4::text || '%')
  )
ORDER BY au.display_name, au.email, au.external_id
LIMIT $6::int
OFFSET $5::int
`

type ListUnlinkedSourceAccountsPageBySourceAndQueryParams struct {
	SourceKind     string `json:"source_kind"`
	SourceName     string `json:"source_name"`
	EntityCategory string `json:"entity_category"`
	Query          string `json:"query"`
	PageOffset     int32  `json:"page_offset"`
	PageLimit      int32  `json:"page_limit"`
}

func (q *Queries) ListUnlinkedSourceAccountsPageBySourceAndQuery(ctx context.Context, arg ListUnlinkedSourceAccountsPageBySourceAndQueryParams) ([]Account, error) {
	rows, err := q.db.Query(ctx, listUnlinkedSourceAccountsPageBySourceAndQuery,
		arg.SourceKind,
		arg.SourceName,
		arg.EntityCategory,
		arg.Query,
		arg.PageOffset,
		arg.PageLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Account
	for rows.Next() {
		var i Account
		if err := rows.Scan(
			&i.ID,
			&i.SourceKind,
			&i.SourceName,
			&i.ExternalID,
			&i.Email,
			&i.DisplayName,
			&i.RawJson,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LastLoginAt,
			&i.LastLoginIp,
			&i.LastLoginRegion,
			&i.SeenInRunID,
			&i.SeenAt,
			&i.LastObservedRunID,
			&i.LastObservedAt,
			&i.ExpiredAt,
			&i.ExpiredRunID,
			&i.Status,
			&i.AccountKind,
			&i.EntityCategory,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertOktaAccountsBulk = `-- name: UpsertOktaAccountsBulk :execrows
WITH input AS (
  SELECT
    i,
    ($3::text[])[i] AS external_id,
    lower(trim(($4::text[])[i])) AS email,
    ($5::text[])[i] AS display_name,
    CASE
      WHEN lower(trim(($6::text[])[i])) IN ('human', 'service', 'bot', 'unknown')
        THEN lower(trim(($6::text[])[i]))
      ELSE 'unknown'
    END AS account_kind,
    CASE
      WHEN lower(trim(($7::text[])[i])) IN ('user', 'group', 'service_principal', 'service_account', 'team', 'role', 'auth_role', 'entity', 'unknown')
        THEN lower(trim(($7::text[])[i]))
      ELSE 'unknown'
    END AS entity_category,
    ($8::text[])[i] AS status,
    ($9::jsonb[])[i] AS raw_json,
    ($10::timestamptz[])[i] AS last_login_at,
    ($11::text[])[i] AS last_login_ip,
    ($12::text[])[i] AS last_login_region
  FROM generate_subscripts($3::text[], 1) AS s(i)
),
dedup AS (
  SELECT DISTINCT ON (external_id)
    external_id,
    email,
    display_name,
    account_kind,
    entity_category,
    status,
    raw_json,
    last_login_at,
    last_login_ip,
    last_login_region
  FROM input
  ORDER BY external_id, i DESC
)
INSERT INTO accounts (
  source_kind,
  source_name,
  external_id,
  email,
  display_name,
  account_kind,
  entity_category,
  status,
  raw_json,
  last_login_at,
  last_login_ip,
  last_login_region,
  seen_in_run_id,
  seen_at,
  updated_at
)
SELECT
  'okta',
  $1::text,
  input.external_id,
  input.email,
  input.display_name,
  input.account_kind,
  input.entity_category,
  input.status,
  input.raw_json,
  input.last_login_at,
  input.last_login_ip,
  input.last_login_region,
  $2::bigint,
  now(),
  now()
FROM dedup input
ON CONFLICT (source_kind, source_name, external_id) DO UPDATE SET
  email = EXCLUDED.email,
  display_name = EXCLUDED.display_name,
  account_kind = EXCLUDED.account_kind,
  entity_category = EXCLUDED.entity_category,
  status = EXCLUDED.status,
  raw_json = EXCLUDED.raw_json,
  last_login_at = COALESCE(EXCLUDED.last_login_at, accounts.last_login_at),
  last_login_ip = COALESCE(NULLIF(EXCLUDED.last_login_ip, ''), accounts.last_login_ip),
  last_login_region = COALESCE(NULLIF(EXCLUDED.last_login_region, ''), accounts.last_login_region),
  seen_in_run_id = EXCLUDED.seen_in_run_id,
  seen_at = EXCLUDED.seen_at,
  updated_at = now()
`

type UpsertOktaAccountsBulkParams struct {
	SourceName       string               `json:"source_name"`
	SeenInRunID      int64                `json:"seen_in_run_id"`
	ExternalIds      []string             `json:"external_ids"`
	Emails           []string             `json:"emails"`
	DisplayNames     []string             `json:"display_names"`
	AccountKinds     []string             `json:"account_kinds"`
	EntityCategories []string             `json:"entity_categories"`
	Statuses         []string             `json:"statuses"`
	RawJsons         [][]byte             `json:"raw_jsons"`
	LastLoginAts     []pgtype.Timestamptz `json:"last_login_ats"`
	LastLoginIps     []string             `json:"last_login_ips"`
	LastLoginRegions []string             `json:"last_login_regions"`
}

func (q *Queries) UpsertOktaAccountsBulk(ctx context.Context, arg UpsertOktaAccountsBulkParams) (int64, error) {
	result, err := q.db.Exec(ctx, upsertOktaAccountsBulk,
		arg.SourceName,
		arg.SeenInRunID,
		arg.ExternalIds,
		arg.Emails,
		arg.DisplayNames,
		arg.AccountKinds,
		arg.EntityCategories,
		arg.Statuses,
		arg.RawJsons,
		arg.LastLoginAts,
		arg.LastLoginIps,
		arg.LastLoginRegions,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const upsertSourceAccountsBulkBySource = `-- name: UpsertSourceAccountsBulkBySource :execrows
WITH input AS (
  SELECT
    i,
    ($4::text[])[i] AS external_id,
    lower(trim(($5::text[])[i])) AS email,
    ($6::text[])[i] AS display_name,
    CASE
      WHEN lower(trim(($7::text[])[i])) IN ('human', 'service', 'bot', 'unknown')
        THEN lower(trim(($7::text[])[i]))
      ELSE 'unknown'
    END AS account_kind,
    CASE
      WHEN lower(trim(($8::text[])[i])) IN ('user', 'group', 'service_principal', 'service_account', 'team', 'role', 'auth_role', 'entity', 'unknown')
        THEN lower(trim(($8::text[])[i]))
      ELSE 'unknown'
    END AS entity_category,
    ($9::jsonb[])[i] AS raw_json,
    ($10::timestamptz[])[i] AS last_login_at,
    ($11::text[])[i] AS last_login_ip,
    ($12::text[])[i] AS last_login_region
  FROM generate_subscripts($4::text[], 1) AS s(i)
),
dedup AS (
  SELECT DISTINCT ON (external_id)
    external_id,
    email,
    display_name,
    account_kind,
    entity_category,
    raw_json,
    last_login_at,
    last_login_ip,
    last_login_region
  FROM input
  ORDER BY external_id, i DESC
)
INSERT INTO accounts (
  source_kind,
  source_name,
  external_id,
  email,
  display_name,
  account_kind,
  entity_category,
  status,
  raw_json,
  last_login_at,
  last_login_ip,
  last_login_region,
  seen_in_run_id,
  seen_at,
  updated_at
)
SELECT
  $1::text,
  $2::text,
  input.external_id,
  input.email,
  input.display_name,
  input.account_kind,
  input.entity_category,
  COALESCE(NULLIF(trim(input.raw_json ->> 'status'), ''), ''),
  input.raw_json,
  input.last_login_at,
  input.last_login_ip,
  input.last_login_region,
  $3::bigint,
  now(),
  now()
FROM dedup input
ON CONFLICT (source_kind, source_name, external_id) DO UPDATE SET
  email = COALESCE(NULLIF(EXCLUDED.email, ''), accounts.email),
  display_name = EXCLUDED.display_name,
  account_kind = EXCLUDED.account_kind,
  entity_category = EXCLUDED.entity_category,
  status = EXCLUDED.status,
  raw_json = EXCLUDED.raw_json,
  last_login_at = COALESCE(EXCLUDED.last_login_at, accounts.last_login_at),
  last_login_ip = CASE WHEN EXCLUDED.last_login_ip <> '' THEN EXCLUDED.last_login_ip ELSE accounts.last_login_ip END,
  last_login_region = CASE WHEN EXCLUDED.last_login_region <> '' THEN EXCLUDED.last_login_region ELSE accounts.last_login_region END,
  seen_in_run_id = EXCLUDED.seen_in_run_id,
  seen_at = EXCLUDED.seen_at,
  updated_at = now()
`

type UpsertSourceAccountsBulkBySourceParams struct {
	SourceKind       string               `json:"source_kind"`
	SourceName       string               `json:"source_name"`
	SeenInRunID      int64                `json:"seen_in_run_id"`
	ExternalIds      []string             `json:"external_ids"`
	Emails           []string             `json:"emails"`
	DisplayNames     []string             `json:"display_names"`
	AccountKinds     []string             `json:"account_kinds"`
	EntityCategories []string             `json:"entity_categories"`
	RawJsons         [][]byte             `json:"raw_jsons"`
	LastLoginAts     []pgtype.Timestamptz `json:"last_login_ats"`
	LastLoginIps     []string             `json:"last_login_ips"`
	LastLoginRegions []string             `json:"last_login_regions"`
}

func (q *Queries) UpsertSourceAccountsBulkBySource(ctx context.Context, arg UpsertSourceAccountsBulkBySourceParams) (int64, error) {
	result, err := q.db.Exec(ctx, upsertSourceAccountsBulkBySource,
		arg.SourceKind,
		arg.SourceName,
		arg.SeenInRunID,
		arg.ExternalIds,
		arg.Emails,
		arg.DisplayNames,
		arg.AccountKinds,
		arg.EntityCategories,
		arg.RawJsons,
		arg.LastLoginAts,
		arg.LastLoginIps,
		arg.LastLoginRegions,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
